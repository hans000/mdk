import { BannerId, BarrelId, BeaconId, BedId, BeeBlockId, BellId, BrewingStandId, CampfireId, ChestId, CommandBlockId, ComparatorId, ConcuitId, DaylightDetectorId, DispenserId, DropperId, EnchantingTableId, EndGatewayId, EndPortalId, FurnaceId, HopperId, JukeboxId, LecternId, ShulkerBoxId, SignId, SkullId, SpawnerId, StructureBlockId } from "./__";

export type BlockEntityId =
    | BannerId
    | BarrelId
    | BeaconId
    | BedId
    | BeeBlockId
    | BellId
    | BrewingStandId
    | CampfireId
    | ChestId
    | CommandBlockId
    | ComparatorId
    | ConcuitId
    | DaylightDetectorId
    | DispenserId
    | DropperId
    | EnchantingTableId
    | EndGatewayId
    | EndPortalId
    | FurnaceId
    | HopperId
    | JukeboxId
    | LecternId
    | ShulkerBoxId
    | SignId
    | SkullId
    | SpawnerId
    | StructureBlockId